[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
multipledispatch = "*"
pandas = "*"
typer = "*"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
server = "python -m flask --app server run"
test = "python -m unittest tests/test_game.py"
