[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
multipledispatch = "*"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
server = "python -m flask --app server run"
